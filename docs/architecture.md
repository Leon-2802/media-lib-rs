# Architecture of the program
## database
### models
Holds the model definitions of the database tables.
### services
For each model there is a service that interacts with the database and serves as a link between database and the backend.
## CLI